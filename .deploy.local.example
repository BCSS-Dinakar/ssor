# Copy to .deploy.local on the server and adjust if needed.
# cp .deploy.local.example .deploy.local

APP_HOST=10.121.9.177
BACKEND_PORT=5000
FRONTEND_PORT=3000
GIT_BRANCH=main

# Optional overrides (auto-derived from APP_HOST and ports when omitted)
# FRONTEND_URL=http://10.121.9.177:3000
# API_BASE_URL=http://10.121.9.177:5000/api

# --- MinIO (deploy.sh ensures this) ---
# ENSURE_MINIO=1
# REQUIRE_MINIO=1          # fail deploy if MinIO is down / creds mismatch
# MINIO_PUBLIC_ENDPOINT=10.121.9.177
# MINIO_PUBLIC_PORT=9000
# MINIO_PUBLIC_USE_SSL=false
# MINIO_RESET_ON_AUTH_FAIL=0   # set 1 only when safe to WIPE MinIO volume
